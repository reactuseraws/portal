exports.validarCampos = async (nomeCampo, conteudoCampo, tipoCampo, callback) => {

    if (tipoCampo = "String") {
      if ( conteudoCampo === "") {
         retValidaCampos = nomeCampo && 'não informado'
      }
    };
  
    callback(retValidaCampos);
  
  };